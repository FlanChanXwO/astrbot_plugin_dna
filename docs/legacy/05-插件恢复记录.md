# 插件恢复记录

## 恢复目标

将 `atri` 上的 DNAUID 插件恢复到 Git 上游 `origin/master`，提交 `b679dba`。该操作会撤销本次排查期间的登录相关本地修改，不删除用户数据表、绑定关系或容器卷。

## 恢复前工作树

恢复前检测到 9 个已修改文件，共 133 行新增、78 行删除：

- `DNAUID/dna_user/login_router.py`
- `DNAUID/dna_user/login_service.py`
- `DNAUID/templates/index.html.j2`
- `DNAUID/utils/api/auth.py`
- `DNAUID/utils/api/request_util.py`
- `DNAUID/utils/api/requests.py`
- `DNAUID/utils/api/sign.py`
- `DNAUID/utils/api/sign_130.py`
- `README.md`

这些修改涉及 App 自动发短信、会话设备码保存、请求对齐与登录说明。恢复的目的不是否定排查结论，而是停止部署未经完整回归验证的登录实验。

## 执行与验证

已完成：

- 已执行 `git fetch --prune origin`，远端 `origin/master` 仍为 `b679dba`。
- 已执行 `git reset --hard origin/master`；恢复后工作树无已跟踪文件改动。
- 已重启 `gsuid_core`；日志显示“插件 DNAUID 导入成功”和“二重螺旋启动完成”。

恢复未执行 `git clean`，以避免删除任何未知的未跟踪文件；恢复前检查中也未发现需要清理的未跟踪项。
